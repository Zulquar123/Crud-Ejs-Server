<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>User Details</title>
</head>
<body>
    <div class="main w-full h-screen bg-gray-400 p-5">
        <div class="menu flex justify-between m-auto w-[40%] p-2 border-2 border-sky-600">
            <a href="/" class="text-xl font-semibold text-white bg-blue-500 p-1 rounded">Create Users</a>
            <a href="/read" class="text-xl font-semibold text-white bg-blue-500 p-1 rounded">Show Users</a>
            
        </div>   
       <div class="container flex gap-2 mt-5 justify-center flex-wrap ">
        <% if(users.length>0){ %>
            <% users.forEach(function(user){%>
                <div class="user w-64 h-96 bg-white rounded flex flex-col items-center">
                    <img src="<%= user.image %>" class="w-64 h-64 rounded-xl p-2 object-cover object-top " alt="profile-pic">
                    <h1 class="text-2xl font-bold">
                        <%= user.name %>
                    </h1>
                    <p class="font-semibold text-gray-600">
                        <%= user.email %>
                    </p>
                    <div class="flex justify-between gap-10 mt-5">
                        <a class="bg-blue-600 text-white font-semibold rounded px-3 py-1" href="/edit/<%= user._id %>">Edit User</a>
                        <a class="bg-blue-600 text-white font-semibold rounded px-3 py-1" href="/delete/<%= user._id %>">Delete
                            User</a>
                    </div>
                </div>
            
                <% }) %>

        <%} else{ %>

            <h1 class="text-4xl text-red-700 font-bold">No User Yet !!!</h1>

        <%} %>

        
      
       </div>
    </div>
</body>
</html>